# virtualenvs (training venv + scripts/venv) — never commit
.venv/
venv/

# generated / regenerated on the training PC (don't commit)
dataset/
data.yaml
runs/

# OAK recording helpers — not needed on the training PC
record.sh
scripts/record_oak.py
scripts/recordings/

# local junk
.cache/
collll.txt

# big media / model artifacts
recordings/
*.mp4
*.onnx
*.blob
*.pt

# NOTE: raw_ciracore/ is intentionally committed — it's the 12MB source data
# the training PC needs to run import_ciracore.py + train.py
